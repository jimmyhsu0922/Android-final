<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Week10Activity"
        tools:targetApi="31">

        <!-- This is your launcher activity, it was already here -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
        ADD THIS BLOCK
        This declares your new SetBirthday activity so the OS can find it.
        -->
        <activity
            android:name=".SetBirthday"
            android:exported="false" />

        <!-- Note: If SetBirthday is in a different package, use the full name -->
        <!-- <activity android:name="com.kaistudio.chap5r.SetBirthday" ... /> -->

    </application>

</manifest>
